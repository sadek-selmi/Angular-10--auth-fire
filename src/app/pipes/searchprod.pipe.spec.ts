import { SearchprodPipe } from './searchprod.pipe';

describe('SearchprodPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchprodPipe();
    expect(pipe).toBeTruthy();
  });
});
